"""VNN-COMP as a plugin for comp-eval-platform.

The whole variant is: one Competition subclass (the six seams) + a handful of
step handlers + the node scripts. No core changes. Django discovers the
AppConfig in apps.py automatically.
"""
